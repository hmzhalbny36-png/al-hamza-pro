[app]

# -----------------------------------------------------------------------------
# الاسم الأساسي للتطبيق
title = Al-Hamza Pro

# -----------------------------------------------------------------------------
# اسم الحزمة
package.name = alhamzapro
package.domain = org.test

# -----------------------------------------------------------------------------
# رقم الإصدار
version = 0.1
version.code = 1

# -----------------------------------------------------------------------------
# المكتبات المطلوبة (Python + Kivy + Pillow)
requirements = python3,kivy,pillow

# -----------------------------------------------------------------------------
# إعدادات الواجهة
orientation = portrait
fullscreen = 0

# -----------------------------------------------------------------------------
# مجلد المصدر (يجب أن يكون موجوداً)
source.dir = .

# -----------------------------------------------------------------------------
# تضمين امتدادات الملفات
source.include_exts = py,png,jpg,kv,atlas

# -----------------------------------------------------------------------------
# الأيقونة (اختياري)
# icon.filename = icon.png

# -----------------------------------------------------------------------------
# الأذونات المطلوبة
android.permissions = INTERNET, WRITE_EXTERNAL_STORAGE, READ_EXTERNAL_STORAGE

# -----------------------------------------------------------------------------
# إعدادات الأندرويد (SDK)
android.minapi = 21
android.sdk = 33
android.gradle_dependencies = ''

# -----------------------------------------------------------------------------
# تحديد مسار SDK الذي ثبتناه عبر apt (الحل السحري لهذه المشكلة)
android.sdk_path = /usr/lib/android-sdk

# -----------------------------------------------------------------------------
# قبول تراخيص SDK تلقائياً (لتجنب أي طلب تأكيد)
android.accept_sdk_license = True

# -----------------------------------------------------------------------------
# إعدادات البناء
[buildozer]
log_level = 2
warn_on_root = 1
