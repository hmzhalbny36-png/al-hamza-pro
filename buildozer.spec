[app]

# -----------------------------------------------------------------------------
# الاسم الأساسي للتطبيق
title = Al-Hamza Pro

# -----------------------------------------------------------------------------
# اسم الحزمة
package.name = alhamzapro
package.domain = org.test

# -----------------------------------------------------------------------------
# رقم الإصدار
version = 0.1
version.code = 1

# -----------------------------------------------------------------------------
# المكتبات المطلوبة (تم إضافة pillow)
requirements = python3,kivy,pillow

# -----------------------------------------------------------------------------
# إعدادات الواجهة
orientation = portrait
fullscreen = 0

# -----------------------------------------------------------------------------
# مجلد المصدر (يجب أن يكون موجوداً)
source.dir = .

# -----------------------------------------------------------------------------
# تضمين امتدادات الملفات (اختياري لكن مفيد)
source.include_exts = py,png,jpg,kv,atlas

# -----------------------------------------------------------------------------
# الأيقونة (اختياري)
# icon.filename = icon.png

# -----------------------------------------------------------------------------
# الأذونات
android.permissions = INTERNET, WRITE_EXTERNAL_STORAGE, READ_EXTERNAL_STORAGE

# -----------------------------------------------------------------------------
# إعدادات الأندرويد
android.minapi = 21
android.sdk = 33
android.gradle_dependencies = ''

# -----------------------------------------------------------------------------
# إعدادات البناء
[buildozer]
log_level = 2
warn_on_root = 1
